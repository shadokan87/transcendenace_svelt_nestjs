generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
	id Int @id @default(autoincrement())
	adminInChannel Channel[]
}

model Channel {
	Id Int @id @default(autoincrement())
	admins User[]
}

